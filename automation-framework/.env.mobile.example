# ─────────────────────────────────────────────────────────────────
# .env.mobile.example — Mobile (Appium) Environment Template
# ─────────────────────────────────────────────────────────────────
# Copy this file to the appropriate .env.mobile.{environment} file:
#
#   cp .env.mobile.example .env.mobile.dev
#   cp .env.mobile.example .env.mobile.staging
#   cp .env.mobile.example .env.mobile.production
#
# NEVER commit the filled .env.mobile.* files to Git.
# Only this template (.env.mobile.example) should be committed.
# ─────────────────────────────────────────────────────────────────

# ── Appium Server ─────────────────────────────────────────────────
# URL where Appium server is running
# Default: http://127.0.0.1:4723
APPIUM_SERVER=http://127.0.0.1:4723

# ── Android ───────────────────────────────────────────────────────
# Name of the connected Android device / emulator
# How to get: run `adb devices` in terminal
ANDROID_DEVICE_NAME=

# Android OS version running on the device (e.g. 14, 15)
# How to get: Device Settings → About Phone → Android Version
ANDROID_PLATFORM_VERSION=

# Android application package name
# How to get: `adb shell pm list packages | grep <app-name>`
ANDROID_APP_PACKAGE=

# Main activity to launch
# How to get: `adb shell dumpsys package <package> | grep -A1 MAIN`
ANDROID_APP_ACTIVITY=

# Automation engine — UiAutomator2 is recommended for Android
ANDROID_AUTOMATION_NAME=UiAutomator2

# ── App Session Behaviour ─────────────────────────────────────────
# NO_RESET=True  → keep app data between sessions (faster, use for dev/staging)
# NO_RESET=False → clear app state before each session (use for production/CI)
NO_RESET=True

# Automatically grant all app permissions on launch (True / False)
AUTO_GRANT_PERMISSIONS=True

# Reset keyboard to default IME before session (True / False)
RESET_KEYBOARD=True

# Disable window animation to speed up tests (True / False)
DISABLE_WINDOW_ANIMATION=True

# ── iOS (planned — leave blank if not used) ───────────────────────
# iOS device name as shown in Xcode / Instruments
IOS_DEVICE_NAME=

# UDID of the connected iOS device
# How to get: `xcrun xctrace list devices` or Xcode → Devices & Simulators
IOS_UDID=

# iOS version running on the device (e.g. 17.0)
IOS_PLATFORM_VERSION=

# Application bundle ID (com.example.app)
# How to get: Xcode project → General → Bundle Identifier
IOS_BUNDLE_ID=

# WebDriverAgent bundle ID (used internally by XCUITest)
# Default: com.facebook.WebDriverAgentRunner
WDA_BUNDLE_ID=com.facebook.WebDriverAgentRunner
