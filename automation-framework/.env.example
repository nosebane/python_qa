# ─────────────────────────────────────────────────────────────────
# .env.example — API & Web Environment Template
# ─────────────────────────────────────────────────────────────────
# Copy this file to the appropriate .env.{environment} file:
#
#   cp .env.example .env.dev
#   cp .env.example .env.staging
#   cp .env.example .env.production
#
# NEVER commit .env.dev / .env.staging / .env.production to Git.
# Only this template file (.env.example) should be committed.
# ─────────────────────────────────────────────────────────────────

# ── API ──────────────────────────────────────────────────────────
# Public API base URL
API_BASE_URL=

# Web base URL
WEB_BASE_URL=

# ── Private API ───────────────────────────────────────────────────
# Base URL for authenticated (TAPI) endpoints
PRIVATE_API_BASE_URL=

# INDODAX Private API credentials
# How to get: Indodax account → API Management → Create Key
INDODAX_API_KEY=
INDODAX_API_SECRET=
